language: node_js
cache: yarn
node_js:
  - stable
  - "8"
  - "6"

script: echo "Running tests against $(node -v)..."

jobs:
  include:
    - stage: Produce Coverage
      node_js: node
      script: jest --coverage --coverageReports=text-lcov | coveralls
